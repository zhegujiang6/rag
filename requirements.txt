# Streamlit 前端
streamlit>=1.31.0

# SQLAlchemy 同步 + pymysql
sqlalchemy>=2.0.25
pymysql>=1.1.0

# 向量存储
chromadb>=0.4.22

# 文档解析
PyMuPDF>=1.23.8
python-docx>=1.1.0
tiktoken>=0.5.2

# HTTP 客户端（LLM/Embedding API调用）
httpx>=0.26.0

# 配置管理
pydantic-settings>=2.1.0

# 日志
loguru>=0.7.2

# 密码哈希（简单认证）
bcrypt>=4.0.0

# ── RAGAS Optimization ──────────────────────────────────────────
# 中文分词（BM25 混合检索）
jieba>=0.42.1
# BM25 关键词检索
rank-bm25>=0.2.2

# ── 网页爬取 ─────────────────────────────────────────────────
readability-lxml>=0.8.1
lxml>=5.0.0
beautifulsoup4>=4.12.0
# 可选: 动态页面渲染（安装后自动启用，需额外运行 playwright install chromium）
# playwright>=1.40.0

# 可选: 交叉编码器重排序（需下载模型 ~2GB）
# sentence-transformers>=2.7.0
# 可选: RAGAS 评测框架
# ragas>=0.1.0

# ── RAG Server (FastAPI 微服务) ───────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
# SSE 流式必需
sse-starlette>=1.8.0
